<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Volunteer Signup</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; line-height: 1.6; color: #0e2f52; max-width: 600px; margin: 0 auto; padding: 20px; background-color: #f8f9fa; }
        .email-container { background: #ffffff; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 15px rgba(9, 135, 201, 0.1); }
        .header { background: linear-gradient(135deg, #7ed956, #0987c9); color: white; padding: 40px 30px; text-align: center; }
        .header h1 { margin: 0; font-size: 28px; font-weight: 700; }
        .header p { margin: 10px 0 0 0; opacity: 0.9; font-size: 16px; }
        .content { padding: 40px 30px; }
        .content p { color: #5a6c7d; margin: 0 0 20px 0; }
        .content p strong { color: #0e2f52; }
        .signup-card { background: #f8f9fa; border-radius: 12px; padding: 25px; margin: 25px 0; border-left: 4px solid #7ed956; }
        .signup-card h2 { margin: 0 0 15px 0; color: #0e2f52; font-size: 18px; }
        .detail-row { display: flex; padding: 8px 0; }
        .detail-label { font-weight: 600; color: #5a6c7d; min-width: 100px; font-size: 14px; }
        .detail-value { color: #0e2f52; }
        .message-box { background: #fef3c7; border-radius: 8px; padding: 15px; margin-top: 15px; border-left: 3px solid #f59e0b; }
        .message-box h4 { margin: 0 0 8px 0; color: #92400e; font-size: 14px; }
        .message-box p { margin: 0; color: #92400e; font-size: 14px; }
        .btn-container { text-align: center; margin: 30px 0; }
        .btn { display: inline-block; background: linear-gradient(135deg, #0987c9, #7ed956); color: white; text-decoration: none; padding: 16px 40px; border-radius: 50px; font-weight: 600; font-size: 16px; box-shadow: 0 4px 15px rgba(9, 135, 201, 0.3); }
        .footer { text-align: center; padding: 25px 30px; background: #f8f9fa; border-top: 1px solid #e5e7eb; }
        .footer p { margin: 0; color: #5a6c7d; font-size: 14px; }
        .footer .brand { color: #0987c9; font-weight: 600; }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <h1>New Volunteer Signup!</h1>
            <p>Someone wants to help</p>
        </div>
        <div class="content">
            <p>A new volunteer has signed up for your event!</p>

            <div class="signup-card">
                <h2><%= eventTitle %></h2>
                <div class="detail-row">
                    <span class="detail-label">Volunteer</span>
                    <span class="detail-value"><%= studentName %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Email</span>
                    <span class="detail-value"><%= studentEmail %></span>
                </div>
                <% if (message) { %>
                <div class="message-box">
                    <h4>Message from volunteer:</h4>
                    <p><%= message %></p>
                </div>
                <% } %>
            </div>

            <div class="btn-container">
                <a href="<%= dashboardUrl %>" class="btn">Manage Signups</a>
            </div>
        </div>
        <div class="footer">
            <p><span class="brand">Volunteens</span> - Connecting young volunteers with opportunities</p>
        </div>
    </div>
</body>
</html>
