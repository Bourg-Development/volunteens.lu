<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup Status Updated</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; line-height: 1.6; color: #0e2f52; max-width: 600px; margin: 0 auto; padding: 20px; background-color: #f8f9fa; }
        .email-container { background: #ffffff; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 15px rgba(9, 135, 201, 0.1); }
        .header { background: linear-gradient(135deg, #0987c9, #7ed956); color: white; padding: 40px 30px; text-align: center; }
        .header h1 { margin: 0; font-size: 28px; font-weight: 700; }
        .header p { margin: 10px 0 0 0; opacity: 0.9; font-size: 16px; }
        .content { padding: 40px 30px; }
        .content p { color: #5a6c7d; margin: 0 0 20px 0; }
        .content p strong { color: #0e2f52; }
        .status-card { background: #f8f9fa; border-radius: 12px; padding: 30px; margin: 25px 0; text-align: center; }
        .status-badge { display: inline-block; padding: 12px 30px; border-radius: 50px; font-size: 18px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; }
        .status-confirmed { background: linear-gradient(135deg, #7ed956, #22c55e); color: white; }
        .status-attended { background: linear-gradient(135deg, #0987c9, #3b82f6); color: white; }
        .status-cancelled { background: linear-gradient(135deg, #e74c3c, #ef4444); color: white; }
        .status-pending { background: linear-gradient(135deg, #f59e0b, #fbbf24); color: white; }
        .event-name { margin-top: 20px; font-size: 16px; color: #5a6c7d; }
        .event-name strong { color: #0e2f52; }
        .notes-box { background: #e0f2fe; border-radius: 8px; padding: 15px; margin-top: 25px; text-align: left; }
        .notes-box h4 { margin: 0 0 8px 0; color: #0369a1; font-size: 14px; }
        .notes-box p { margin: 0; color: #0369a1; font-size: 14px; }
        .message-box { background: #dcfce7; border-radius: 8px; padding: 15px; margin-top: 20px; text-align: left; }
        .message-box p { margin: 0; color: #166534; font-size: 14px; }
        .footer { text-align: center; padding: 25px 30px; background: #f8f9fa; border-top: 1px solid #e5e7eb; }
        .footer p { margin: 0; color: #5a6c7d; font-size: 14px; }
        .footer .brand { color: #0987c9; font-weight: 600; }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <h1>Status Update</h1>
            <p>Your signup status has changed</p>
        </div>
        <div class="content">
            <p>Hi <strong><%= studentName %></strong>,</p>
            <p>Your signup status for the event below has been updated:</p>

            <div class="status-card">
                <span class="status-badge status-<%= status %>"><%= status %></span>
                <p class="event-name">Event: <strong><%= eventTitle %></strong></p>
            </div>

            <% if (notes) { %>
            <div class="notes-box">
                <h4>Note from organizer:</h4>
                <p><%= notes %></p>
            </div>
            <% } %>

            <% if (status === 'confirmed') { %>
            <div class="message-box">
                <p>Great news! Your participation has been confirmed. We look forward to seeing you at the event!</p>
            </div>
            <% } else if (status === 'attended') { %>
            <div class="message-box">
                <p>Thank you for volunteering! Your attendance has been recorded and your invoice will be generated shortly.</p>
            </div>
            <% } %>
        </div>
        <div class="footer">
            <p><span class="brand">Volunteens</span> - Connecting young volunteers with opportunities</p>
        </div>
    </div>
</body>
</html>
